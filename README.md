# SLPM - SoloLinux Package Manager
The package manager for SoloLinux

## Installation
- Copy slpm to /usr/local/bin/slpm
- Make executable
- Run `sudo slpm init`
- Run `sudo slpm install [package name]` (It uses pacman's package database)
